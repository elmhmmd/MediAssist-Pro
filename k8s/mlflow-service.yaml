apiVersion: v1
kind: Service
metadata:
  name: mlflow-service
  namespace: mediassist
spec:
  selector:
    app: mlflow-server
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30050
  type: NodePort
